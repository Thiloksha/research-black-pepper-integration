import React from 'react';
import { View } from 'react-native';

export default function BottomNav() {
  // A placeholder to prevent the app from crashing due to missing import.
  // The MainTabNavigator usually handles the bottom navigation.
  return <View />;
}
